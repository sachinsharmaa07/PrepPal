POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=judge0pass
REDIS_PASSWORD=judge0redis

# Disable auth for local dev (enable + set token for anything beyond localhost)
AUTHN_HEADER=
AUTHN_TOKEN=

ENABLE_WAIT_RESULT=true
ENABLE_COMPILER_OPTIONS=true
ALLOWED_LANGUAGES_FOR_COMPILE_OPTIONS=
ENABLE_COMMAND_LINE_ARGUMENTS=true
MAX_QUEUE_SIZE=100
POSTGRES_HOST=db
REDIS_HOST=redis
